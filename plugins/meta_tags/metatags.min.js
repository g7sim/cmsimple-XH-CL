(function(){"use strict";function on(target,event,listener){if(typeof target.addEventListener!=="undefined"){target.addEventListener(event,listener,false);}else if(typeof target.attachEvent!=="undefined"){target.attachEvent("on"+event,listener);}}
on(window,"load",function(){var form=document.getElementById("meta_tags"),title=form.elements.title,titleIndicator=document.getElementById("mt_title_length"),description=form.elements.description,descIndicator=document.getElementById("mt_description_length");if(title&&titleIndicator){XH.displayTextLength(title,titleIndicator);}
if(description&&descIndicator){XH.displayTextLength(description,descIndicator);}});}());